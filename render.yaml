services:
  - type: web
    name: census-income-classifier-api
    runtime: python
    rootDir: starter
    buildCommand: pip install -r requirements.txt && pip install -e .
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeployTrigger: checksPass
    healthCheckPath: /
